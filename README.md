# Hexadecimal-To-Decimal
A simple Java console application that converts a single hexadecimal character (0-9, A-F) into its decimal equivalent.

## Example Output
```text
Enter a hexadecimal value: B
The decimal value for hexadecimal digit B is 11
